//Create a Turtle class whose constructor will take two arguments (in order): x & y coordinates
const arr=[];
class turtle{
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
   

    forward(n) {
        this.x = this.x+n;
        this.y = this.y;
    }
}

    

    